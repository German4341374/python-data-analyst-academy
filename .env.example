# Local-only defaults. Change both secrets before any shared deployment.
ACADEMY_PORT=8080
POSTGRES_PASSWORD=local-development-only
RUNNER_TOKEN=local-runner-development-only
# Set to 1 behind a properly configured HTTPS proxy.
COOKIE_SECURE=0
